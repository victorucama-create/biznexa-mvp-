# Este arquivo é OPcional se configurar pelo painel do Render
services:
  - type: web
    name: biznexa-backend
    env: php
    region: ohio
    rootDirectory: backend
    buildCommand: chmod +x ./.render/build.sh && ./.render/build.sh
    startCommand: php artisan serve --host=0.0.0.0 --port=${PORT}
    healthCheckPath: /api/health
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: LOG_CHANNEL
        value: stderr
    autoDeploy: true
    disk:
      name: storage
      mountPath: /opt/render/project/src/backend/storage
      sizeGB: 1
